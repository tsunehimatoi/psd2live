# 操作速查

[文档目录](../../README.md) · [English](../../en/guide/USER_GUIDE.md) · [日本語](../../ja/guide/USER_GUIDE.md)

先打开 **帮助 → 教程…**。交互教程会定位控件、切换相关面板，并显示你当前设置的快捷键；本文仅供回看，不重复逐按钮讲解。

## 最短流程

导入 PSD（`Ctrl+Shift+O`）→ 检查图层分类与预览 → 按需编辑 → 保存工程（`Ctrl+S`）→ 导出（`Ctrl+G`）。已有 `.psd2live` 用 `Ctrl+O` 打开。

## 按交互教程学习

| 课 | 主题 | 操作要点 |
| --- | --- | --- |
| 1 | 基础流程 | 导入分层 PSD，检查预览、图层预设和模型设置，再选择导出格式。 |
| 2 | 工作区导览 | 编辑页改模型，预览页看效果，历史页切版本；每个画布标签页独立保存镜头和视图开关。 |
| 3 | 层级树与模式条 | 在树中选择、搜索或改父级；透明图片可拖入树中，放置确认后成为图层。绘制顺序决定遮挡，父级决定继承形变。 |
| 4 | 图层类型与差分 | 「预设」使用部件算法；「开关差分」用一个参数控制有无；「切换差分」用同一参数及不同关联 ID 实现多选一。 |
| 5 | 参数与关键帧 | 拖滑块或 XY 控件检查姿态；右键关键帧标记吸附。先确定目标参数值，再编辑形状。 |
| 6 | 选择模式 | 选择对象，利用框选、套索和树画布联动定位；只选对象，不修改几何。 |
| 7 | 创建变形器 | 选目标 → 树右键添加父级或子级变形器 → 调整放置预览 → 确认。 |
| 8 | 变形模式 | 调整当前姿态的控制点、整体变换或形变笔刷；注意当前 L1 / L2 编辑层级。 |
| 9 | 编辑模式 | 修改网格拓扑，例如细分、连接、切割与删除；完成后复查参数姿态。 |
| 10 | 绘画模式 | 先选图层，再用画笔、橡皮、填充等修像素；会话内独立撤销，完成后应用或放弃。 |
| 11 | 检视面板 | 根据所选对象修改名称、归属、遮罩、绘制顺序、透明度与颜色等属性。 |
| 12 | 工具细节 | 调整当前工具的半径、硬度、强度、形状等；右键菜单也随模式和工具变化。 |
| 13 | 工程与历史 | 保存 `.psd2live`，在历史树恢复节点或从旧节点继续形成分支；隐藏分支不删除内容。 |
| 14 | 贴图高清化 | 配置本地后端，选择 2× / 4×；检查边缘、半透明部件和导出结果。 |

## 三个容易混淆的操作

- **保存与导出**：保存保留工作区和历史；导出生成目标软件使用的模型文件。`.psd2live.json` 是报告，不是工程。
- **变形与编辑**：变形改变形状；编辑改变控制网格的结构。绘画修改像素，采用独立的应用 / 放弃会话。
- **显隐与差分**：树中的临时独显、图层静态显隐用于编辑检查；需要随参数切换时，设置差分或不透明度关键形。

## 常用操作

下列为默认键位；设置中的快捷键预设或自定义绑定会改变它们。`F1` 帮助显示当前绑定。

| 操作 | 默认键位 |
| --- | --- |
| 打开工程 / 导入 PSD | `Ctrl+O` / `Ctrl+Shift+O` |
| 保存 / 另存为 | `Ctrl+S` / `Ctrl+Shift+S` |
| 撤销 / 重做 | `Ctrl+Z` / `Ctrl+Y` 或 `Ctrl+Shift+Z` |
| 导出模型 / 重新导出 PSD | `Ctrl+G` / `Ctrl+Shift+E` |
| 缩放 / 平移画布 | 滚轮 / 中键拖动或空格 + 左键 |
| 居中适配 | `F` / `Home` / `0` |
| 确认 / 取消当前草稿 | `Enter` / `Esc`，具体行为以当前工具提示为准 |

## 检查与排错

模型不符合预期时，先核对[图层命名](../spec/PSD_LAYER_SPEC.md)、部件侧别、遮罩和父级，再调整参数与模型设置。动态问题应在动画或物理面板观察，单个静态姿态不能说明完整运动效果。

面板不见时检查布局开关，必要时重置布局；看不见模型时先适配画布，再检查显隐。导出失败或降级时查看日志和导出报告。提交问题请附版本、系统、复现步骤及相关素材。

进一步阅读：[画布编辑](CANVAS_EDITOR.md) · [变形路径](DEFORM_PATHS.md) · [高清化](TEXTURE_UPSCALE.md) · [SDK](CUBISM_SDK_SETUP.md) · [Agent / MCP](../agent/MCP_AUTHORING.md) · [构建与 CLI](DEVELOPMENT.md)。

维护依据：[交互教程定义](../../../src/main/kotlin/io/github/psd2live/ui/tutorial/InteractiveTutorial.kt)、[中文教程文案](../../../src/main/resources/i18n/Messages_zh_CN.properties)、[快捷键注册表](../../../src/main/kotlin/io/github/psd2live/ui/state/ShortcutRegistry.kt)。
